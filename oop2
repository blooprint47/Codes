//sol for: https://codeforces.com/group/c3FDl9EUi9/contest/262795/problem/J

#include <bits/stdc++.h>
#define tes int test=0;cin>>test;while(test--)
#define A7MAD ios_base::sync_with_stdio(NULL); cin.tie(0); cout.tie(0);
typedef long long ll;
using namespace std;

struct person{

    string name;
    int a,m,s,e,total;
    //void init(){total=a+m+s+e;}

    bool operator<(const person &other)const
    {
        if(total==other.total) return name<other.name;
        return total>other.total;
    }
};

int main() {
    A7MAD
        int n;cin>>n;
        person p[n];
        for(int i=0;i<n;i++)
        {
            cin>>p[i].name>>p[i].a>>p[i].m>>p[i].s>>p[i].e;
            p[i].total=p[i].a+p[i].m+p[i].s+p[i].e;
        }
        sort(p,p+n);
        for(auto it:p)
            cout<<it.name<<' '<<it.total<<' '<<it.a<<' '<<it.m<<' '<<it.s<<' '<<it.e<<'\n';
     return 0;
}
//easy shi












